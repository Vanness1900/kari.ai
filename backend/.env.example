OPENAI_API_KEY=
GOOGLE_API_KEY=

CHROMA_DB_PATH=./chroma_db
UPLOADS_PATH=./uploads
SESSIONS_PATH=./sessions

DEFAULT_STUDENT_MODEL=gemini-3.1-flash-lite
# Teacher model is fixed in agents/teacher.py (gpt-5.5).
DEFAULT_REASONING_MODEL=gpt-5.5

# Uncomment when insight visual generation is wired
# ENABLE_VISUAL_REPORT=true

# Comma-separated if you need multiple dev origins
CORS_ORIGINS=http://localhost:3000
